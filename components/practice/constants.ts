/** Shared layout constants for the mahjong table grid */

/** Size of the MahjongMat SVG in px */
export const MAT_SIZE = 367;

/** Inner content area of the mat (for discard piles etc.) */
export const MAT_INNER_SIZE = 305;

/** Offset from mat edge to inner content area */
export const MAT_INNER_OFFSET = 31;

/** Width of the corner grid columns */
export const GRID_COL = 120;

/** Negative margin pulling player areas toward the mat */
export const GRID_MARGIN = -170;

/** translateY applied to top/bottom player rows to tuck them against the mat */
export const GRID_TRANSLATE_Y = 14;
